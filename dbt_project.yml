name: "ci_for_dbt"
version: "1.0.0"
config-version: 2

profile: "ci_for_dbt"

# Paths
model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

clean-targets:
  - "target"
  - "dbt_packages"

# Model defaults
models:
  ci_for_dbt:
    staging:
      +materialized: view
    marts:
      +materialized: table
