version: 2

# ----------------------------------------------------------------------------
# TODO (à toi) — remplis ce fichier avec :
#   1. Des tests génériques (not_null, unique, relationships, accepted_values)
#      sur les colonnes-clés. Vise au moins 5 tests au total sur les deux
#      modèles.
#   2. Un CONTRAT sur dim_customers qui déclare toutes les colonnes avec
#      leurs types. C'est ce que la CI va tester : si tu changes un type
#      ou supprimes une colonne, le build doit échouer.
#
# Référence sur les contrats :
#   https://docs.getdbt.com/reference/resource-configs/contract
#
# Squelette d'exemple (supprime et remplace avec tes vraies déclarations) :
#
#   - name: dim_customers
#     config:
#       contract:
#         enforced: true
#     columns:
#       - name: customer_id
#         data_type: varchar
#         data_tests:
#           - not_null
#           - unique
#       - name: ...
# ----------------------------------------------------------------------------

models:
  - name: dim_customers
    description: "Dimension clients, une ligne par customer_id, enrichie avec les agrégats de commandes."
    # TODO : ajouter contract + colonnes + tests

  - name: fct_orders
    description: "Fait commandes, une ligne par order_id, enrichi avec le pays du client."
    # TODO : ajouter colonnes + tests (minimum : not_null + unique sur order_id,
    # relationships vers dim_customers sur customer_id, accepted_values sur status)
