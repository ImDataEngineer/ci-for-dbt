version: 2

sources:
  - name: raw
    description: "Tables sources chargées depuis les seeds CSV."
    schema: main
    tables:
      - name: raw_customers
        description: "Base clients. Une ligne par client, clé customer_id."
      - name: raw_orders
        description: "Commandes passées par les clients. Une ligne par commande."
